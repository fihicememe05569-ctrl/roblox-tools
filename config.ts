interface Config {  apiUrl: string;  timeout: number;  retries: number;}const defaultConfig: Config = {  apiUrl: 'https://api.example.com',  timeout: 5000,  retries: 3};function loadConfig(customConfig: Partial<Config>): Config {  return { ...defaultConfig, ...customConfig };}export { loadConfig, Config };