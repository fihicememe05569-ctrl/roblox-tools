export async function retry<T>(operation: () => Promise<T>, attempts: number = 3, delay: number = 1000): Promise<T> { let lastError; for (let i = 0; i < attempts; i++) { try { return await operation(); } catch (error) { lastError = error; if (i < attempts - 1) { await new Promise(resolve => setTimeout(resolve, delay)); } } } throw lastError; } export const fetchWithRetry = async (url: string, options?: RequestInit, attempts: number = 3): Promise<Response> => { return retry(() => fetch(url, options), attempts); };