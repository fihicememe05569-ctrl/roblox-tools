function validateInput(input: any): boolean {    if (typeof input !== 'string' || input.trim() === '') {        console.error('Invalid input: Must be a non-empty string');        return false;    }    return true;}function mainProcessingLoop(inputs: any[]): void {    for (const input of inputs) {        if (!validateInput(input)) {            continue;        }        processInput(input);    }}function processInput(input: string): void {    // Main processing logic here    console.log(`Processing: ${input}`);}export { mainProcessingLoop, validateInput };